# ═══════════════════════════════════════════════════
# KrishiKonnect Backend — Python Dependencies
# FastAPI + Firebase + Anthropic + Agricultural Stack
# ═══════════════════════════════════════════════════

# ── Web Framework ──────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9       # file uploads
aiofiles==23.2.1               # async file handling

# ── Authentication & Security ──────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
PyJWT==2.8.0

# ── Database / User Records ────────────────────────
firebase-admin==6.5.0          # Firestore for user auth & visit tracking
sqlalchemy==2.0.30             # optional local SQLite for caching
aiosqlite==0.20.0

# ── AI / LLM ──────────────────────────────────────
anthropic==0.29.0              # Claude API (primary LLM)
google-generativeai==0.7.2     # Gemini fallback
openai==1.35.3                 # OpenAI fallback (chatbot LLM switcher)
httpx==0.27.0                  # async HTTP for direct API calls

# ── Image Processing (VLM upload) ─────────────────
Pillow==10.3.0                 # image resize / validate before base64
python-magic==0.4.27           # MIME type validation

# ── Agricultural Data ─────────────────────────────
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1                  # curve fitting for decay models

# ── PDF Generation (agronomist report) ────────────
reportlab==4.2.0               # programmatic PDF
weasyprint==62.3               # HTML-to-PDF (alternative)

# ── Environment ───────────────────────────────────
python-dotenv==1.0.1

# ── Rate Limiting & Middleware ─────────────────────
slowapi==0.1.9                 # rate limiting
starlette==0.37.2

# ── Logging ───────────────────────────────────────
loguru==0.7.2

# ── Date / Time ───────────────────────────────────
python-dateutil==2.9.0
pytz==2024.1

# ── CORS / Security Headers ───────────────────────
secure==0.3.0
